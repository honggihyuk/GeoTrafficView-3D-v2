# GeoTrafficView-3D — 기술 문서 III

## 학습 도메인 교체 — VisDrone에서 국내 CCTV로

> **I권**([TECHNICAL_DOCS.md](TECHNICAL_DOCS.md))은 초기 구축, **II권**([TECHNICAL_DOCS_2.md](TECHNICAL_DOCS_2.md))은
> 정밀도로지도 정렬과 평가 체계를 다룹니다.
> **이 문서**는 세 번째 층입니다: 탐지기가 학습한 **도메인 자체를 교체**하는 작업.
>
> I권에서 우리는 "모델 확대보다 기법 교체가 4배 효과적"이라는 결론을 얻었고, 그때의 기법이
> **VisDrone 파인튜닝 + SAHI**였습니다. 이 문서는 그중 **VisDrone을 걷어내는** 과정입니다.
> SAHI는 남습니다.
>
> 방법이 아니라 **개념**을 적었습니다 — 무엇을 왜 바꾸려 했고, 그 과정에서 어떤 함정을
> 발견했으며, 무엇이 열리고 무엇이 막혔는지.

---

## 0. 한눈에

| 영역 | 무엇을 알아냈나 | 상태 |
|---|---|---|
| 도메인 진단 | VisDrone의 불일치는 성능이 아니라 **시점** | 학습 믹스에서 제외 확정 |
| 오염 이론 | 오염에 **두 종류**가 있고 대처가 다르다 | 리맵 명세로 형식화 |
| 역-감독 | 라벨 부재보다 **잘못된 라벨**이 더 해롭다 | 마스킹으로 차단 |
| 프라이어 | VisDrone 치수에 **실제 오류 2건** | `measurements_kr` 신설 |
| 모델 | YOLO26이 소형 객체를 겨냥해 나옴 | 8.4 계열로 전환 |
| Stage A | 도로변 시점 사전학습 데이터 118,229장 | 학습 진행 중 |
| Stage B | AI Hub 164 확보, 다만 **9%만 수신** | 대기 |
| trailer 분리 | 준비 완료, 그러나 **165 없이는 학습 불가** | 봉인 |

---

## 1. 왜 VisDrone을 걷어내는가

### 1.1 불일치는 성능 문제가 아니다

VisDrone 파인튜닝은 I권에서 검출을 1,830 → 7,904로 끌어올린 선택이었습니다. 그 판단은
당시 조건에서 옳았습니다. 문제는 **그 데이터가 무엇을 가르치는가**입니다.

| 축 | VisDrone | 우리 대상(ITS/TOPIS 고속도로 CCTV) |
|---|---|---|
| 시점 | UAV 10~100m, 준-천저 | 갓길 폴 8~10m, **저각 사선** |
| 택소노미 | tricycle, awning-tricycle, van (중국 도심) | 삼륜차는 국내 고속도로에 없음 |
| 조건 | 주간·맑음 편중 | 야간 22%, 우천·안개·설상, 터널 진출입 |

세 축 모두 어긋나지만, **셋의 성격이 다릅니다.** 이 구분이 이 문서 전체의 골격입니다.

### 1.2 프라이어에 남아 있던 실제 오류

도메인 불일치는 추상적인 이야기가 아니라 3D 박스 치수로 이미 새어 나오고 있었습니다.

| 항목 | 기존(VisDrone 계열) | 실제 국내 기준 | 성격 |
|---|---|---|---|
| car 길이 | 3.8 m | **4.80 m** | 중국 소형차 기준. 국내 중형은 4.7~5.0m |
| bus 폭 | 3.0 m | **2.49 m** | **국내 법정 상한 2.5m 초과** — 물리적으로 불가능한 값 |
| truck 길이 | 5.5 m | 7.60 m | 1톤~대형 카고 혼합 |

car 길이 3.8m는 3D 박스가 실차보다 1m 짧게 그려지고 있었다는 뜻입니다. bus 폭 3.0m는
자동차관리법상 존재할 수 없는 차량입니다. **모델을 바꾸지 않아도 고쳐야 했던 값**이고,
도메인 교체를 검토하지 않았다면 발견되지 않았을 것입니다.

`three_wheeler`(1.3×2.7×1.75)는 제거했습니다. 국내 고속도로에서 이 박스가 뜨면 100% 오검출인데,
3D 리프팅은 그 치수로 실제 박스를 그립니다.

---

## 2. 오염에는 두 종류가 있다

> **이 절이 이 문서의 중심 개념입니다.**

여러 데이터셋을 합칠 때 "클래스 이름만 맞추면 된다"고 생각하기 쉽습니다. 실제로는 성격이
다른 두 종류의 불일치가 있고, **하나는 고칠 수 있고 하나는 못 고칩니다.**

### 2.1 택소노미 충돌 — 리맵으로 교정 가능

AI Hub 164의 `car`는 **승용차 + SUV + 밴 + 픽업**을 전부 포함합니다. 그런데 VisDrone은
밴을 `van`이라는 별도 클래스로 답니다. UA-DETRAC도, MIO-TCD도 각자 다르게 자릅니다.

같은 승합밴에 데이터셋마다 다른 정답이 붙는 상태 — 모델에 모순된 감독신호를 주는 것이고
명백한 오염입니다. **다만 이건 접기(folding)로 해소됩니다.** `van → car`로 정규화하면
충돌이 사라집니다.

### 2.2 시점 충돌 — 리맵으로 교정 불가

VisDrone의 진짜 문제는 이쪽입니다. UAV 준-천저 시점이 학습시키는 **종횡비와 원근 단서**는
갓길 폴 저각 사선과 근본적으로 다릅니다. 위에서 내려다본 차량은 지붕이 보이고 박스가
정사각에 가깝지만, 도로변 카메라는 측면과 전후면이 보이고 원근 압축이 심합니다.

라벨을 아무리 손봐도 픽셀이 바뀌지 않습니다. **교정 불가능한 오염**입니다.

### 2.3 그래서 결론이 갈린다

| 데이터셋 | 택소노미 충돌 | 시점 충돌 | 판정 |
|---|---|---|---|
| AI Hub 164/165 | 없음(기준) | 없음 | 타깃 도메인 |
| UA-DETRAC | 있음 | **없음** | 리맵 후 사전학습 |
| MIO-TCD | 있음 | **없음** | 리맵 후 사전학습 |
| VisDrone | 있음 | **있음** | **제외** |

VisDrone을 버리는 이유는 클래스가 안 맞아서가 아니라 **시점이 안 맞아서**입니다.
같은 이유로 UA-DETRAC·MIO-TCD는 클래스가 안 맞아도 **살립니다** — 그건 고칠 수 있으니까.

이 판정을 사람의 기억이 아니라 **명세 파일**(`datasets/class_map_kr.yaml`)에 박아 두었습니다.
제외 사유와 접기 규칙이 데이터로 남아 있어야, 나중에 누가 "VisDrone도 넣지 그래"라고 할 때
근거를 다시 만들지 않아도 됩니다.

---

## 3. 라벨 부재보다 잘못된 라벨이 해롭다

> **이 문서에서 가장 반직관적인 부분입니다.**

### 3.1 역-감독(negative supervision)

어떤 클래스가 데이터셋에 없으면 "그 클래스를 못 배운다"고 생각하기 쉽습니다. 실제로는
더 나쁜 일이 일어납니다.

UA-DETRAC의 차종은 car / bus / van / **others** 4종뿐입니다. truck 클래스가 아예 없습니다.
중국 고속도로 영상이니 트럭은 분명히 찍혀 있고, 그건 `others`에 들어가 있습니다.

`others`를 그냥 버리면 어떻게 될까요. 트럭이 화면에 **있는데 라벨이 없는** 상태가 됩니다.
검출기 학습에서 라벨 없는 영역은 배경입니다. 즉 **"트럭처럼 생긴 것은 배경이다"를 명시적으로
가르칩니다.** 단순 결측이 아니라 능동적인 오학습입니다.

### 3.2 같은 함정이 세 곳에서 반복된다

이 패턴은 데이터셋마다 다른 옷을 입고 나타납니다.

| 데이터셋 | 문제 클래스 | 실체 | 그냥 버리면 |
|---|---|---|---|
| UA-DETRAC | `others` | 트럭·특수차 혼재 | truck을 배경으로 학습 |
| MIO-TCD | `motorized_vehicle` | **가림·시점 때문에 차종 특정 불가**(공식 정의) | 실제 차량 25,845대를 배경으로 학습 |
| AI Hub 164 | `truck` | 트레일러 포함 혼합 라벨 | trailer를 truck으로 되돌림(§6) |

MIO-TCD의 `motorized_vehicle`은 특히 명확합니다 — 데이터셋 README가 직접 "가림이나 시점 때문에
특정 차종으로 분류할 수 없는 차량"이라고 정의합니다. **이들은 실제로는 car/truck/bus 중 하나**입니다.
버리면 차량 25,845대가 배경 표본이 됩니다.

### 3.3 해법의 개념 — 지우는 게 아니라 가린다

세 가지 선택지가 있습니다.

1. **억지로 매핑** — `others → truck`. 잡동사니를 truck에 주입하므로 다른 종류의 오염.
2. **그냥 버림** — 위에서 본 역-감독.
3. **가림(masking)** — 해당 영역을 덮어 학습에서 배제.

3번을 택했습니다. 개념적으로 이건 **"모른다"를 명시하는 행위**입니다. 검출기에는
"여기 아무것도 없다"와 "여기 뭔가 있는데 무엇인지 모른다"를 구분할 문법이 없으므로,
후자를 픽셀 수준에서 제거해 질문 자체를 없앱니다.

덮는 색은 임의의 검정이 아니라 YOLO의 레터박스 패딩값을 씁니다. 모델이 이미 "내용 없음"으로
학습해 온 값이라 인공적 경계가 덜 생깁니다.

같은 처리를 UA-DETRAC의 `ignored_region`(데이터셋이 스스로 "여기는 주석을 보장하지 않는다"고
선언한 영역)에도 적용합니다. 성격이 정확히 같은 문제입니다.

### 3.4 실측 규모

| 소스 | 가린 이미지 | 가린 영역 |
|---|---|---|
| UA-DETRAC train | 7,371 | 30,405 |
| UA-DETRAC val | 2,825 | 11,897 |
| MIO-TCD train | 27,153 | 39,420 |

MIO-TCD 39,420개 중 25,845개가 `motorized_vehicle`입니다. 이 처리가 없었다면 그만큼의
차량이 배경 음성으로 들어갔습니다.

### 3.5 부수 원칙 — 커버리지를 줄이는 모든 동작은 로그에 남긴다

리맵 폐기, 파서 자체 폐기(ignored_region), 클리핑 후 소멸 — 사유별로 나눠 집계합니다.
조용한 누락이 곧 오염이기 때문입니다. 매핑에 없는 클래스를 만나면 drop으로 흘려보내지 않고
**즉시 중단**합니다. 학습 데이터 오염의 대부분은 "몰랐던 클래스를 조용히 버린" 데서 옵니다.

---

## 4. 클래스를 쪼개면 남은 클래스도 바뀐다

혼합 클래스에서 한 종류를 분리하면, **분리된 쪽만이 아니라 남은 쪽의 프라이어도 재보정**해야
합니다. 이 점이 자주 누락됩니다.

`measurements_kr`의 `truck`(7.60m)은 1톤 포터부터 트랙터-트레일러까지 전부 섞인 혼합값입니다.
여기서 trailer를 떼어내면 남은 화물차의 평균 길이는 **짧아집니다**. 그런데 프라이어를
그대로 두면 일반 화물차가 과대 추정됩니다.

| | trailer 분리 전 | 분리 후 |
|---|---|---|
| truck | 2.30 × 7.60 × 2.95 | **2.15 × 6.80 × 2.75** |
| trailer | — | 2.49 × 16.0 × 3.90 |

trailer는 트랙터+40ft 샤시 기준이고, 연결자동차 법정 길이 상한 16.7m·높이 상한 4.0m 안에
들어갑니다.

개념적으로: **프라이어는 클래스 정의에 종속적**입니다. 택소노미를 바꾸면 치수표는 자동으로
낡습니다. 이걸 놓치면 "클래스를 세분화했는데 3D 박스가 더 나빠졌다"는 결과가 나옵니다.

---

## 5. 혼합이 아니라 순차 — 2단계 학습

### 5.1 볼륨 비대칭

해외 도로변 데이터는 국내 데이터보다 **훨씬 큽니다**. MIO-TCD 110,000장 + UA-DETRAC(원본
138,252프레임)에 비해 AI Hub 164의 검출용 수신분은 24,101프레임입니다.

단순 혼합하면 타깃 도메인이 소수파가 됩니다. 사전학습의 이점을 얻으려다 정작 맞추려는
도메인이 희석되는 역설입니다.

### 5.2 구조

- **Stage A** — 리맵된 해외 도로변 데이터로 사전학습.
  얻는 것: 고정 카메라 저각 시점, 야간·악천후, 원근 압축된 차량 형상.
- **Stage B** — AI Hub 164 단독 파인튜닝.
  얻는 것: 국내 차종 분포, 국내 도로 구조, ITS 인코딩 특성.

혼합이 아니라 **순차**인 이유는 마지막에 본 데이터가 모델을 지배하기 때문입니다.
Stage B를 국내 데이터만으로 두어야 도메인이 국내로 수렴합니다.

### 5.3 Stage A의 구성 — truck이 곧 목적이었다

| 소스 | 이미지 | 기여 |
|---|---|---|
| MIO-TCD | 110,000 | car·truck·bus 전 클래스 |
| UA-DETRAC | 8,229 | car·bus (**truck 0**) |
| **합계 train** | **118,229** | |
| val (UA-DETRAC Test) | 2,825 | |

UA-DETRAC 단독 Stage A의 클래스 분포는 **car 94.5% / bus 5.5% / truck 0%** 였습니다.
사실상 car 단일 클래스 사전학습입니다. MIO-TCD를 넣은 유일한 목적이 truck이었고,

| | car | truck | bus |
|---|---|---|---|
| UA-DETRAC 단독 | 94.54% | **0%** | 5.46% |
| + MIO-TCD | 92.2% | **4.1%** | 3.7% |

여전히 car 편중이지만 truck이 0에서 벗어났습니다. 이건 "데이터를 더 넣었다"가 아니라
**"없던 클래스를 만들었다"** 입니다.

### 5.4 상태

Stage A 학습이 진행 중입니다. epoch 1에서 mAP50 0.877 / mAP50-95 0.614.
**이 수치는 대부분 car 성능**이라는 점을 유념해야 합니다 — 클래스별 분해 없이는 의미가 제한적입니다.

---

## 6. trailer 분리 — 준비됐지만 봉인된 것

### 6.1 왜 막혔나

trailer를 별도 클래스로 분리하려면 trailer 라벨이 필요합니다. AI Hub 164는 3종(car/truck/bus)
뿐이라 실제 트레일러가 전부 `truck`으로 라벨돼 있습니다.

MIO-TCD의 `articulated_truck`으로 Stage A에서 trailer를 배울 수는 있습니다. 그런데
**Stage B(164)에서 같은 트레일러 픽셀에 "이건 truck이다"라는 정답이 주어집니다.**

§3의 역-감독이 순차 학습 구조 때문에 더 확실하게 작동합니다. Stage A의 trailer 지식을
Stage B가 능동적으로 지웁니다.

### 6.2 개념적 결론

**trailer 분리는 데이터 문제가 아니라 라벨 체계 문제입니다.** 프라이어·택소노미·리맵은
전부 준비돼 있고, 막힌 건 "국내 도메인에서 trailer를 trailer라고 부르는 라벨"의 부재입니다.

해소 경로는 셋입니다. ① AI Hub 165 도입(대형트레일러가 별도 라벨) ② 164의 truck 부분 재라벨
③ Stage B에서 해당 클래스 손실 마스킹. ③은 trailer가 국내 도메인 적응을 못 받으므로 차선책입니다.

명세에서 164의 v2 매핑을 `__conflict__`로 표시해, 이 상태로는 빌드 자체가 거부되게 했습니다.
**틀린 조합이 조용히 성립하지 않도록 막는 것**이 요점입니다.

---

## 7. 모델 세대 — YOLO26

데이터 교체와 별개로 모델 세대도 올렸습니다. 다만 우선순위는 명확합니다: I권의 결론대로
**도메인 교체가 모델 확대보다 큰 레버**이고, 모델 교체는 그 위의 작은 이득입니다.

| | mAP50-95 | 비고 |
|---|---|---|
| YOLO11s (기존) | 47.0 | |
| YOLO26s | **48.6** | GPU 지연시간 동일 |

우리 케이스에 의미 있는 건 벤치마크 점수보다 **설계 의도**입니다. YOLO26은 소형 객체 대응을
명시적 목표로 삼았고(ProgLoss + 소형 타깃 인지 라벨 할당), 우리 파이프라인은 원거리 소형
차량이 대부분입니다.

주의할 점도 있습니다. NMS-free end-to-end 헤드에서는 `iou`·`agnostic_nms` 같은 후처리
파라미터의 의미가 달라지고 최대 검출 수 동작도 바뀔 수 있습니다. 밀집 장면 설정은
**실측 검증 대상**으로 남습니다. SAHI 슬라이스 간 병합은 모델 밖 후처리라 영향받지 않습니다.

---

## 8. 검증한 사실 (실측)

문서 근거가 아니라 실제 파일을 열어 확인한 것들입니다.

**UA-DETRAC에 truck이 없다** — train 60시퀀스 598,281박스의 분포가
car 84.22% / van 9.54% / bus 5.62% / others 0.62%. truck 클래스 자체가 존재하지 않습니다.

**AI Hub 164는 세로형 영상을 포함한다** — XML이 `width=1080 height=1920`으로 선언해 좌표
뒤바뀜을 의심했으나, 실제 PNG 헤더와 대조한 결과 일치했습니다. 진짜 세로형 CCTV입니다.
**파서는 이미지별 해상도를 읽어야 하며 고정값을 가정하면 안 됩니다.**

**AI Hub 164 클래스는 선언부에 명시돼 있다** — CVAT 1.1 포맷의 라벨 선언이 `truck`/`bus`/`car`
3종으로, 명세의 매핑과 일치합니다. 추측이 아닙니다.

**파일명에 카메라 메타데이터가 있다** — 지점·채널·일시·요일·**카메라 높이**·러시아워 여부·
날씨·해상도가 인코딩돼 있습니다. 카메라 높이는 G-Projection 캘리브레이션(II권 §3)에
직접 쓸 수 있는 값입니다.

---

## 9. 수정한 판단

> II권 §10의 관례를 따릅니다. 중간에 틀렸던 것을 남깁니다.

**① "재주석판 UA-DETRAC이 truck 문제를 해결할 수 있다" — 기각**

클래스가 재주석된 배포본이 있어 truck 라벨을 기대했으나, 클래스 목록·설명이 일절 없었습니다.
표본에서 인덱스 `{2,3,4,8}`이 관측됐지만 각각이 무엇인지 확인할 방법이 없습니다.
**의미를 검증할 수 없는 라벨은 이 문서가 막으려는 오염 그 자체**이므로 채택하지 않았습니다.

**② "폴리곤 세그멘테이션 원천을 보존해야 한다" — 이후 철회**

폴리곤 라벨은 min/max로 bbox 변환이 가능하고, 이미지 커버리지가 바운딩박스 서브셋보다
넓었습니다(271클립/31,010프레임 대 168클립/24,101프레임). 보존을 권고했으나 디스크가
병목이 되면서 계산이 바뀌었습니다. **라벨은 보존하고 원천만 삭제**해, 필요해지면 원천만
다시 받아 되살릴 수 있게 남겼습니다.

**③ "AI Hub 164를 받으면 Stage B를 시작할 수 있다" — 조건부로 정정**

라벨은 7개 권역 전부 수신됐으나 원천(이미지)은 수도권영동선 일부뿐입니다.

| | 라벨 클립 | 이미지 있는 클립 |
|---|---|---|
| Training | 1,889 | **168** (8.9%) |
| Validation | 239 | **1** (0.4%) |

**Validation은 터널 클립 1개(144프레임)뿐이라 검증셋으로 쓸 수 없습니다.** 학습(수도권 주간
고속도로)과 도메인도 어긋납니다. Training을 클립 단위로 분할하는 편이 낫고, 클립 단위여야
같은 영상의 인접 프레임이 train/val에 섞이는 누수도 막힙니다.

---

## 10. 제약

| 항목 | 내용 | 영향 |
|---|---|---|
| **MIO-TCD 라이선스** | CC BY-NC-SA 4.0 (비상업 + 동일조건변경허락) | Stage A 가중치가 NC에 묶이고, Stage B로 **전파**됨 |
| **상업화 ↔ truck 상충** | 상업화하려면 MIO-TCD 제외 → Stage A의 truck이 0%로 복귀 | 현재 데이터 구성에서 **양립 불가** |
| AI Hub 이용조건 | 내국인 한정, 상업적 이용은 수행기관 별도 협의 | 위 상충의 유일한 출구인데 같은 질문이 반복됨 |
| 수신 완성도 | 검출용 원천 8.9%, Validation 사실상 없음 | Stage B 규모 제한 |
| trailer | 165 없이는 학습 불가(§6) | v2 봉인 |
| 클래스 균형 | Stage A가 car 92.2% | 지표가 car 성능에 지배됨 |
| 평가 | Stage A/B 성능을 **국내 GT로 비교한 적 없음** | 개선 주장은 아직 근거 없음 |

라이선스 상충은 이 문서에서 가장 구조적인 제약입니다. 기술로 우회되지 않고, **AI Hub 승인
조건이 결정합니다.**

---

## 11. 열린 가능성

**AI Hub 165 (시내도로)** — TOPIS/UTIC 시내 지점의 타깃 도메인이면서, 동시에 trailer 봉인을
푸는 유일한 국내 라벨원입니다. 7종 택소노미(승용차·소형버스·대형버스·트럭·**대형트레일러**·
오토바이·보행자)에 **Tracking 어노테이션**이 포함돼, II권 §5에서 배경차분으로 대신했던
추적 GT를 실제 track_id로 대체할 수 있습니다.

**폴리곤 → bbox** — 라벨을 보존해 두었으므로 원천만 다시 받으면 검출 데이터를 2배 이상으로
늘릴 수 있습니다. 폴리곤에서 유도한 박스는 사람이 그린 박스보다 경계가 정확합니다.

**카메라 높이 메타데이터** — 파일명에 인코딩된 설치 높이는 II권 §3의 캘리브레이션 검수
게이트에 바로 쓸 수 있는 사전값입니다. 자세 분해가 물리적으로 타당한지 판정할 때
독립적인 기준이 됩니다.

**국내 도메인 평가 체계** — II권에서 만든 GT 라벨링·평가 도구를 그대로 Stage B 검증에
쓸 수 있습니다. 지금까지의 모든 판단은 **데이터 분포에 근거**했고, 실제 성능 비교는
아직 하지 않았습니다.

---

## 12. 다음 작업

1. **AI Hub 164 원천 수신 완료** — 최소한 수도권영동선 전량. Stage B의 선행조건
2. **Validation 재구성** — Training을 클립 단위로 분할(프레임 단위 분할은 누수)
3. **Stage B 파인튜닝** — 국내 도메인 수렴
4. **국내 GT 비교** — VisDrone 기존 모델 대 Stage B 모델. **이 문서의 모든 주장이 여기서 검증됨**
5. **AI Hub 165 도입 판단** — trailer 분리·추적 GT·시내 도메인이 전부 여기 묶임
6. **클래스별 지표 분해** — car 92%가 전체 mAP를 지배하므로 총계는 의미가 제한적
7. **NMS-free 헤드 실측** — 밀집 장면에서 최대 검출 수·후처리 파라미터 거동 확인
8. **라이선스 결정** — 상업화 계획이 서면 Stage A를 MIO-TCD 없이 재구성해야 함

---

## 부록. 이 문서가 남긴 산출물

| 산출물 | 성격 |
|---|---|
| `datasets/class_map_kr.yaml` | 리맵 명세. 제외 사유·접기 규칙·라이선스·선행조건을 **데이터로** 보존 |
| `prior_dimensions.json` → `measurements_kr` | 국내 기준 치수. 법정 제원 상한 반영 |
| `prior_dimensions.json` → `measurements_kr_v2` | trailer 분리판(봉인 상태) |
| `inference_config.yaml` → `kr_cctv` 계열 | 국내 도메인 추론 프로파일 |

명세를 파일로 남긴 이유는, **판단의 근거가 사람의 기억에만 있으면 재현되지 않기 때문**입니다.
"VisDrone은 왜 뺐는가", "others는 왜 버리면 안 되는가", "164로 trailer를 왜 못 만드는가" —
전부 명세 안에 사유와 함께 적혀 있고, 위반하면 빌드가 거부됩니다.
