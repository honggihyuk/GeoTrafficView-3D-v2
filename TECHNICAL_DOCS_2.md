# GeoTrafficView-3D — 기술 문서 II
## 정밀도로지도 차로 정렬 · 자동 캘리브레이션 · 평가 체계

> **I권**([TECHNICAL_DOCS.md](TECHNICAL_DOCS.md))은 초기 구축 — 수집·탐지·기하변환·웹맵·DB·LLM을 다룹니다.
> **이 문서**는 그 위에 올린 두 번째 층입니다: 정밀도로지도를 실제로 물려 **차로 단위로 정렬**하고,
> 캘리브레이션을 **자동화·검수**하고, 그 결과를 **GT로 검증**하는 체계.
>
> 이 문서의 모든 수치는 실행해서 얻은 값입니다. **효과가 없었던 것도 그대로 적었습니다** —
> 어떤 시도가 왜 실패했는지가 다음 판단의 근거이기 때문입니다.

---

## 0. 한눈에

| 영역 | 무엇을 했나 | 결과 |
|---|---|---|
| HD맵 | 6개 데이터셋 병합 · 주행방향 검증 | 역방향 **0건**(전 데이터셋) · 카메라 3대 커버 |
| 차로 정렬 | 링크 방향 1차 근거 · HMM 맵매칭 · 차로 고정 | 차선 이탈 **0** · 플리커 **0/s** · s 후진 **0** |
| 캘리브레이션 | 검수 게이트 · 노면표시 자동 정합 | 잔차 17.3→**8.5px** · 다만 GCP 1~3점 필요 |
| 정적 오탐 | 박힌 안내문구 마스크 | 정밀도 0.394→**0.683**, 재현율 불변 |
| 평가 | 근거리 GT 라벨링 | **BEV 재추적이 ByteTrack보다 나쁨**(이전 주장 철회) |
| 학습 | 회전 카운팅 · 운동 필드 | 구조는 완성, **표본 부족**(녹화 시간 문제) |
| 표출 | deck.gl 3D 차량 + Frenet 보간 | 키프레임 재현 오차 **0.000000m** |

---

## 1. 정밀도로지도 통합

### 1.1 보유 현황

국토정보플랫폼 배포본 6종을 확인했습니다. `A2_LINK.shp`가 **117개**(좌표계 판본 포함), 프로젝트
기준 좌표계(UTM52N 타원체고) 판본만 세면 **39구간**입니다.

- 인천송도 시범운행지구 · 인천광역시도 9호선(송도-항동선)
- 고속국도 1호선(경부선, 20구간) · 100호선(서울외곽순환선, 14구간) · 120호선(경인선)
- 자율주행시범지구 판교

### 1.2 커버리지는 bbox로 판단하면 안 된다

경부선처럼 긴 노선은 bbox가 거대해서, bbox만 보면 4대가 커버되는 것처럼 보입니다.
**실제 링크 지오메트리까지의 거리**로 재면 다릅니다.

| 카메라 | 최근접 A2_LINK | 데이터셋 |
|---|---|---|
| PANGYO_2 | **10 m** | 경부선 SEC01_양재IC_서울TG |
| YEONSU_JCT | **61 m** | 송도-항동선 SEC001 |
| SONGDO_L020101 | 커버 | 송도 시범지구 |
| OKRYEON_IC | 1,161 m | — |
| SONGDO_IC | 1,451 m | — |
| SANGAM01 | 없음 | 상암 3D 정밀도로지도 필요 |

> `lane_snap`은 원래 "2km 안에 링크가 있으면 HD맵 채택"이었습니다. 그 결과 SONGDO_IC에서
> **스냅률 0%로 조용히 실패**했습니다. 원점 150m 이내 게이트를 넣어 커버리지 밖이면 OSM으로
> 넘어가게 했습니다.

### 1.3 A2_LINK 주행 방향 — 검증 결과

정밀도로지도의 `A2_LINK`는 **차로 단위 방향성 링크**이고, 지오메트리 진행 방향 =
`FromNodeID → ToNodeID` = 주행 방향입니다. 이 규약을 `A1_NODE` 좌표로 검증했습니다.

```
송도 시범지구      898개 확인 · 역방향 0
송도-항동선       1491개 확인 · 역방향 0
경부선             639개 확인 · 역방향 0
서울외곽순환선     1195개 확인 · 역방향 0
판교 시범지구      618개 확인 · 역방향 0
```

**전 데이터셋에서 역방향 0건.** 관측 트랙 없이 HD맵 자체 토폴로지만으로 성립하므로,
**정지 차량의 정면/후면도 확정할 수 있습니다.**

### 1.4 다중 데이터셋 병합 — `tools/export_hdmap_snap.py`

배포본이 노선/지구별로 쪼개져 있고 카메라마다 덮는 데이터셋이 다르므로, 루트를 재귀 탐색해
**등록 카메라를 덮는 구간만** 병합합니다. 카메라 반경 밖 피처는 버리므로 경부선 전체를
지정해도 결과가 작습니다.

```bash
python tools/export_hdmap_snap.py            # 등록 카메라 전부 자동
python tools/export_hdmap_snap.py --loc PANGYO_2 --radius 1500
```

```
섹션 탐색: 전체 39개 중 카메라 주변 후보 7개
A2_LINK 1615 · A1_NODE 1540 · B2 2694 · B3 986 · C1 321  (정점 126,972개, 5.2 MB)

카메라별 커버리지(반경 800m):
  PANGYO_2        605링크  ★ 차로 정렬 가능
  SONGDO_L020101  745링크  ★
  YEONSU_JCT      265링크  ★
  OKRYEON_IC / SONGDO_IC / SANGAM01   0링크  — OSM 폴백
```

보존 속성: `lane_no`, `from_node`, `to_node`, `r_link`, `l_link`, `road_rank`, `link_type`,
`length`, `dir_fixed`. B3_SURFACEMARK(횡단보도·화살표)도 포함해 `calibrate.html`의 GCP 스냅
대상에 넣었습니다 — 횡단보도 모서리는 영상에서도 또렷해 최상급 GCP입니다.

---

## 2. 차로 정렬 — 방향 확정과 이탈 방지

### 2.1 교차로에서 최근접 스냅은 틀린다

교차로 내부에는 직진·좌회전·우회전 링크가 **물리적으로 교차**합니다. 링크 위 점 300개를
테스트한 결과:

- **48건(16%)** 이 1m 이내 거리에서 방위가 140° 이상 다른 링크에 붙음
- 그중 **31건은 완전 역방향**

거리만으로는 구분할 수 없습니다. 링크 선택 비용에 방향 일관성을 넣어 해결했습니다 —
후보를 링크당 1개씩 여러 개 받아 `거리 + dir_weight·(1−cos Δ)/2`로 고릅니다. 기준 방향은
자기 관측이 있으면 그것을, 정지 프레임이면 **그 트랙이 직전까지 달리던 방향**을 씁니다.

| | 방위오차 평균 | >90°(역방향) |
|---|---|---|
| 최근접만 | 18.77° | 31건 |
| 방향 일관성 | **1.94°** | **0건** |

### 2.2 HD맵 ↔ OSM 채점 폴백

YEONSU_JCT에서 HD맵 스냅이 **방향차 89.5°** 로 나왔습니다. 캘리브레이션 문제가 아니라
**HD맵에 이 카메라가 보는 도로가 없고 직교하는 교차 도로만 있었던 것**입니다.

그래서 `auto` 모드를 "HD맵 있으면 무조건"에서 **두 후보를 같은 기준으로 채점 후 선택**으로
바꿨습니다. 계산과 반영을 분리해 진 쪽은 객체를 건드리지 않습니다.

```
후보 ① HD맵 A2_LINK … 스냅률 36% · 방향차 중앙값 89.0°
  → 품질 미달. OSM 후보와 비교합니다.
후보 ② OSM 중심선 … 스냅률 99% · 방향차 중앙값 2.2°
채택: OSM 중심선
```

### 2.3 HMM 맵매칭 — `tools/mapmatch.py`

프레임별 독립 선택(그리디)은 나란한 차로 사이를 깜빡입니다. 트랙 전체를 Viterbi로 한 번에
풉니다(Newson & Krumm 2009).

- **방출확률** 횡거리 가우시안 + 방향 벌점
- **전이확률** `|직선거리 − 경로거리|` 지수분포 + 연결성 게이트

| 카메라 | 링크 플리커/s (그리디→Viterbi) |
|---|---|
| PANGYO_2 | 1.20 → **0.12** (90%↓) |
| SONGDO_IC | 0.72 → **0.27** (63%↓) |
| YEONSU_JCT | 0.30 → **0.15** (49%↓) |

> **잡은 버그**: 역추적이 트랙 재시작 지점에서 중단돼 **관측의 20%가 소실**되고 있었습니다.
> 재시작을 만나면 그 앞 구간을 독립 세그먼트로 다시 역추적하도록 고쳤습니다.

### 2.4 차로 고정(lane lock) — 이탈을 구조적으로 막는다

맵매칭 출력을 뜯어보니 여전히 문제가 있었습니다.

```
링크 전환 29건 → 전부 횡방향 점프(도로를 따라 갈 수 없는 전환) · 종방향 0건
같은 링크 내 s 후진 122건
횡오프셋 |d| 중앙 1.38m · 95% 10.65m
```

세 가지를 각각 막았습니다.

**① 차로 사슬 고정** — Viterbi 결과 중 도로를 따라 실제로 이어지는 **가장 긴 구간**만 남기고
나머지 관측을 그 사슬 위로 다시 투영합니다. 후보 선택 자체가 없어져 나란한 차로로 새는
경로가 원천 차단됩니다.

**② s 단조화(PAVA 등장성 회귀)** — 후진을 없앱니다. `running max`로 자르면 차가 멈췄다
따라잡는데, 등장성 회귀는 위배 구간을 평균으로 눌러 자연스럽게 폅니다.

**③ 속도 평활 후 재적분** — ②만 쓰니 **프레임쌍의 33.9%가 Δs=0**이 되어 차가 멈췄다 튀었습니다.
속도를 이동평균한 뒤 다시 적분하면 단조성은 유지하면서 정지·급변이 사라집니다.
총 이동거리는 보존합니다.

| | 적용 전 | 적용 후 |
|---|---|---|
| 횡방향 점프 | 29건 | **0건** |
| s 후진 | 122건 | **0건** |
| 횡오프셋 \|d\| 95% | 10.65 m | **0.000 m** |
| 링크 플리커 | 1.45/s | **0.00/s** |
| heading 지터 p90 | 0.39° | **0.18°** |
| 속도 변화 \|Δ\| 중앙 | 10.8 km/h/frame | **2.16** |
| 정지 프레임 | 33.9% | **12.2%** |

남은 12.2% 정지 구간은 원본 속도 중앙 19.8 km/h(이동 구간은 32.0)로 **실제로 느리거나 서 있는
차량**입니다.

> **한계**: 이건 차로 이탈을 **못 하게 만든 것**이지 **올바른 차로를 고른 것**이 아닙니다.
> 캘리브레이션이 부정확하면 실제와 다른 차로에 놓입니다. 또 한 트랙은 한 사슬에 머물러
> **차로 변경·회전을 표현하지 않습니다**. 긴 녹화에서 차로 변경을 보려면 인접 차로
> (`r_link`/`l_link`)로만 전환을 허용하는 규칙이 필요합니다 — 속성은 이미 있습니다.

---

## 3. 캘리브레이션

### 3.1 검수 게이트 — `tools/lane_overlay.py`

차선을 영상으로 **순투영**해 도로 위에 얹히는지 봅니다. 역투영(image→ground)은 지평선
근처에서 발산하지만 순투영은 어디서나 안정적입니다.

```bash
python tools/lane_overlay.py --loc PANGYO_2
```

**5대 중 4대가 탈락했습니다** — 차선이 도로가 아니라 하늘과 건물을 가로지릅니다.

종방향 해상도도 함께 냅니다(PANGYO_2): 영상 1px = 지상 0.16~1.27 m. 차량 길이 4.4m 대비
충분하므로 **픽셀 공간에서 차선을 배정해도 됩니다.**

### 3.2 자세 분해가 곧 타당성 검사

`H = K·[r1|r2|t]` 분해는 카메라 높이를 그냥 내놓습니다. 물리적으로 불가능하면 그 H는
실재하는 카메라 자세에 대응하지 않는다는 뜻입니다.

| 카메라 | 분해 높이 | GCP | 캘리브 방식 | 판정 |
|---|---|---|---|---|
| PANGYO_2 | **38.2 m** | 10 | 자동 VP+IPM | 비현실적 |
| SONGDO_IC | 15.3 m | 4 | GCP 실측 | 타당 · HD맵 시야 밖 |
| YEONSU_JCT | 8.3 m | 10 | 자동 placeholder | 타당 |
| OKRYEON_IC | **−2.7 m** | 7 | 자동 VP+IPM | 비현실적(지면 아래) |
| SANGAM01 | 11.6 m | 0 | placeholder | 타당 · HD맵 없음 |
| SONGDO_L020101 | 11.8 m | 0 | PLACEHOLDER | 타당 |

고속도로 CCTV 폴은 10~15m입니다. **자동 VP+IPM 캘리브레이션이 부정확한 정도가 아니라
구조적으로 틀렸다**는 뜻이고, 하류가 전부 어긋나는 이유가 여기서 설명됩니다.
3줄이면 나오는 진단이니 검수 항목에 넣을 만합니다.

### 3.3 노면표시 자동 정합 — `tools/autocalib_lanes.py`

영상에도 차선이 보이고 정밀도로지도에도 차선이 있으니 둘을 직접 맞춥니다.
위성영상↔HD맵에 쓴 Chamfer/DT를 **CCTV영상↔HD맵**으로 옮긴 것입니다.

**전제 조건 3가지 — 하나라도 빠지면 수렴하지 않습니다.**

1. **배경 중앙값 프레임** — 단일 프레임은 차량이 차선을 가립니다.
2. **정적 오탐 마스크** — 박힌 안내문구("부산/경부선/판교2/407")가 밝고 가늘어 top-hat이
   그대로 잡습니다. 첫 시도에서 마스크의 **대부분이 글자**였고 최적화가 차선이 아니라
   **글자에 맞추려 들었습니다**.
3. **도로 영역 제한(검출 기반)** — 이게 결정적이었습니다. 차량 검출이 지나간 자리가 곧
   도로이고, 이건 캘리브레이션과 무관한 신호입니다. 화면 전체를 대상으로 하면 가드레일
   줄무늬·식생 경계가 top-hat 반응의 대부분을 차지해 백분위 임계가 잡동사니에 맞춰집니다.
   → **마스크 1,513px → 9,865px**, 그리고 그게 실제 차선.

**매개변수화**: 현재 H에서 자세를 분해하고 **증분 회전(Rodrigues) 3 + 이동 3 + 초점 1 = 7자유도**로
최적화합니다. 오일러 각 규약을 새로 정의하지 않아도 됩니다.

```
정합 전 잔차 17.26px
① 자세 거친 탐색(pitch/yaw 2축, 다중 시작점)
② Powell 7자유도, 상위 4개 시작점: → 8.88px
③ 8자유도 정밀화: → 8.54px (4%)
곡면 대비 27.9% · 비등방 2.0:1 · 신뢰 가능
```

### 3.4 목적함수 결함 3개 — 전부 실측으로 발견

Chamfer 정합은 **지표가 좋아지면서 결과가 붕괴하는** 방식으로 속입니다.

| # | 증상 | 원인 | 대응 |
|---|---|---|---|
| ① | 잔차 개선인데 점이 18,680→1,443개로 감소 | 화면 안 점만 평균 → 밖으로 밀면 이득 | 평가 집합 고정 + 화면 밖 최대 벌점 |
| ② | 잔차 40%↓인데 투영이 화면 중앙 소실점으로 붕괴 | 단방향 Chamfer → 페인트 밀집 영역으로 뭉개기 | **양방향 Chamfer** + **깊이 양수 제약** |
| ③ | Powell이 경계값 반환 | 그 방향이 구속되지 않음 | 경계 근접 시 정밀화 기각 |

②는 제가 지표만 믿고 `--apply`한 뒤 발견했고 **git으로 되돌렸습니다.** 이후로는
`lane_overlay` 게이트로 눈으로 확인되지 않으면 적용하지 않습니다.

### 3.5 8자유도는 이득이 없다 — 그리고 그게 정보다

7 → 8자유도로 얻은 것이 **4%뿐**이고 물리성(`K⁻¹H` 앞 두 열의 정규직교성)은 나빠졌습니다.
**남은 잔차는 투영 오차가 아닙니다.**

두 가지를 더 측정했습니다.

- **잔차가 거리와 무관하게 균일** (0–50m 11.3px / 50–100m 13.1px / 100–150m 11.7px /
  150–200m 10.9px) → 원거리 발산이 아니라 전 구간 체계적 오차
- **횡방향 비용 곡면이 평탄** — HD맵 전체를 도로 수직으로 ±8.75m 밀어도 비용 변동 **3%**

나란한 선 무리끼리는 어떻게 겹쳐도 Chamfer 점수가 비슷해집니다. **페인트가 횡방향 위치를
구속하지 못합니다.**

### 3.6 결론 — 자동 정합은 거친 정렬까지

정합 전 "우측 차도 선이 중앙분리대를 가로질러 건물로" → 정합 후 "도로 방향과 나란함"으로
**구조는 바로잡히지만** 차로 단위 정확도에는 못 미칩니다.

**중요한 점**: 자동 정합은 **원근 구조 오차(1~3점 GCP로 못 고침)를 지면 평면 오차
(`correction3`로 정확히 고침)로 바꿔 줍니다.** 그래서 다음 단계가 성립합니다.

`calibrate.html`의 하이브리드 경로는 **1점=평행이동, 2점 이상=유사변환**을 기존 H에 곱합니다.

```
① python tools/autocalib_lanes.py --loc <CAM> --apply     # 구조 정렬
② calibrate.html 에서 GCP 1~3점 클릭 → [계산 & 저장]        # 지면 앵커
```

> ⚠ **4점 이상 클릭하면** 전체 재계산 경로로 빠져 자동 정합 결과가 버려집니다.

### 3.7 GCP 후보 추천 — `tools/gcp_candidates.py`

지도 쪽은 🧲 HD맵 스냅이 cm급으로 해결했으니 남은 어려움은 **영상 쪽**입니다.

**선정 기준**: ① 모서리형만(차선 중간 점은 선을 따라 미끄러짐 — 조리개 문제)
② 지면 위만(신호등은 공중이라 제외) ③ 3×3 구역에 고르게(잔차 0의 함정 방지).

`calibrate.html`에 `🎯 HD맵 GCP 후보 안내` 체크박스를 추가했습니다. 켜면 번호 마커가 뜨고
**영상만 클릭**하면 지도 쪽 좌표를 HD맵에서 그대로 가져옵니다 — 위성 눈대중 클릭 오차
(1~2m)가 통째로 사라지고 클릭 수도 절반입니다.

> **현재 한계**: 캘리브레이션 정밀도가 낮으면 예측 점이 빈 아스팔트를 가리켜 점 단위
> 추천이 도움이 안 됩니다. 대신 **화면에 걸리는 모든 노면표시를 컨텍스트로 함께 그려**
> "예측된 선 무리 ↔ 실제 선 무리"로 거칠게 맞춘 뒤 정밀화하는 2단계로 쓰는 게 실용적입니다.

### 3.8 HD맵 ↔ 위성영상 정합 — `tools/hdmap_align.py`

카메라 캘리브레이션과는 별개로, **지도 위에 얹은 HD맵이 위성 basemap과 어긋나는지**도 잽니다.
위성영상에서 노면 페인트를 추출해 거리변환하고, HD맵 노면표시를 워프하며 Chamfer를 최소화합니다.

| 지역 | 편위 | 곡면 대비 | 1m 인라이어 | 판정 |
|---|---|---|---|---|
| 판교(경부선) | **0.80 m** | 17.5% | 26.2→30.5% | 신뢰 가능 · 워프 불필요 |
| 송도 시범지구 | **1.69 m** | 10.1% | 18.7→22.7% | 신뢰 가능 |
| 연수JCT | 13.0 m | 5.1% (비등방 2.7:1) | 16.3→16.2% | **신뢰 불가** |

**결론: 휨(러버시팅)은 필요 없습니다.** Esri World Imagery와 정밀도로지도의 편위는 1~2 m이고
국소 왜곡 중앙값은 0.5 m입니다. 회전·스케일은 잔차 이하라 노이즈이므로 **평행이동만** 씁니다.

`hdmap.geojson`은 건드리지 않고 `hdmap_align.json`만 씁니다 — **HD맵이 더 정확한 쪽**이라
측량 성과를 상용 모자이크에 맞춰 휘면 절대정확도가 나빠집니다. 표시 단계에서만 적용하고
`lane_snap`·DB에는 넣지 마세요.

> 연수JCT처럼 직선 도로만 있는 AOI는 정합이 성립하지 않습니다(조리개 문제).
> 정지선·횡단보도가 있는 교차로를 AOI에 포함시켜야 합니다.

---

## 4. 정적 오탐 마스크 — `tools/static_mask.py`

### 4.1 문제

ITS/TOPIS 영상은 지점명·방면·거리를 **인코딩 전에 합성**합니다. YOLO는 이 글자 덩어리를
차량으로 잡습니다. 고정 위치 반복 검출이 전체 검출의 **11.8%**였습니다.

### 4.2 정지 차량과의 구분 — 사람 승인을 남긴 이유

둘 다 같은 자리에 계속 검출됩니다. 판별 근거는 **시간축 표준편차**입니다 — 박힌 문구는
합성된 평면 그래픽이라 인코더가 거의 그대로 복사합니다.

다만 이 신호만으로 자동 확정하지 않습니다. **카메라당 1회 설정**이므로 후보를 크롭 이미지로
제시하고 사람이 승인합니다.

**이 장치가 실제로 작동했습니다.** SANGAM01의 후보 5개(최대 112×136px)를 보니 정체된
승용차와 버스였습니다. 클립이 2초뿐이라 신호 대기 차량이 정적으로 보인 것입니다.
마스크를 만들지 않았고, 클립이 10초 미만이면 경고합니다.

### 4.3 결과 — GT로 검증

| | AP50 | P | R | MOTA | IDF1 |
|---|---|---|---|---|---|
| 원본 | 0.555 | 0.394 | 0.596 | **−0.362** | 0.441 |
| 마스크 | 0.562 | **0.683** | **0.596** | **+0.277** | **0.591** |

**재현율이 0.596으로 한 톨도 안 떨어졌습니다** — 실제 차량은 하나도 지우지 않았습니다.
MOTA가 음수에서 양수로 넘어갔습니다.

| 카메라 | 승인 | 제거 | 내용 |
|---|---|---|---|
| PANGYO_2 | 2 | 203 (3.6%) | "서**울**", "판**교**2" |
| SONGDO_IC | 4 | 1,725 (21.8%) | 안내 기호 ×3, "38" |
| YEONSU_JCT | 1 | 95 (2.5%) | 안내 기호 |
| OKRYEON_IC | 2 | 182 (19.4%) | "31", 안내 기호 |
| SANGAM01 | **0** | — | 후보 전부 실제 차량 |

마스크 영역별 제거 빈도가 프레임당 0.98~1.00 — **거의 매 프레임 잡히던 정적 그래픽**입니다.
지나가는 차량이면 간헐적이어야 합니다.

`trafficlab/inference/pipeline.py`가 `location/<LOC>/static_mask.png`를 자동으로 읽어
추론 단계에서 바로 제외합니다(박스의 60% 이상이 덮일 때만 제거 → 글자 옆을 스치는 실제
차량은 살아남습니다).

---

## 5. 평가 — GT 라벨링과 그 한계

### 5.1 반자동 라벨링

720×480에서 원거리 차량은 중앙 11×8px입니다. 눈으로 찍으면 주석 오차가 객체 크기와
맞먹어, 그 GT로 추적 변경을 검증하면 **주석 잡음을 측정**하게 됩니다.

**배경 차분**(프레임 중앙값)으로 박스를 제안했습니다 — 평가 대상 검출기(YOLO)와 완전히
독립적인 신호이고 경계가 사람 눈대중보다 정확합니다. 사람은 몽타주로 전수 검증하고
동일성을 판단합니다(실제로 track 8↔9가 동일 차량인데 끊긴 것을 찾아 병합).

- 47박스 · 9트랙 · 29프레임 · ROI `[355, 275, 695, 445]`
- `evaluate.py --roi`를 추가해 **예측도 같은 ROI로 제한** — 안 그러면 라벨 안 한 영역의
  정상 검출이 FP로 잡힙니다
- 수집 ROI 경계에 걸려 잘린 박스는 제외하고, 평가 ROI를 25px 안쪽으로 둬 해당 차량의
  예측도 함께 제외

### 5.2 결과 — 이전 주장의 철회

| | AP50 | P | R | MOTA | IDF1 | IDSW |
|---|---|---|---|---|---|---|
| A. 기존 ByteTrack | 0.555 | 0.394 | 0.596 | **−0.362** | **0.441** | **2** |
| B. BEV (age30/90) | 0.555 | 0.394 | 0.596 | −0.426 | 0.390 | 5 |
| C. BEV+차로 (age90) | 0.555 | 0.394 | 0.596 | −0.404 | 0.407 | 4 |

**"수명 중앙값 0.92→3.07초 개선"은 추적 품질 개선의 근거가 되지 못합니다.** 라벨된 근거리
구간에서 BEV 재추적은 IDF1 0.441→0.390, ID 스위치 2→5로 **더 나쁩니다.** 수명 증가의 일부는
잘못된 병합이었고, 프록시 지표(>180km/h 연관)로는 못 잡았습니다.

단서 둘: ① 표본이 작습니다(IDSW 2 vs 5는 3건 차이) ② **BEV의 원래 주장은 원거리에서
이미지 IoU가 무너진다는 것인데 라벨은 근거리만 있습니다.**

### 5.3 원거리 GT는 만들 수 없다 — 시도와 근거

세 가지를 시도했고 전부 막혔습니다.

1. **배경차분 반자동** — 원거리는 다수 누락·인접 차량 병합·표지판 허위 박스
2. **중거리로 후퇴** — f0~f12는 두 차량을 하나로 병합, **f15~f27은 차량이 또렷한데 제안 0개**.
   그 차로가 상시 점유돼 **배경 중앙값 자체에 차량이 섞였습니다.** 임계 조정으로 해결 불가
3. **육안 직접** — 6배 확대해도 10px 블록 뭉개짐. 객체 경계·차량/노면표시 구분 불가

원본 mp4가 720×480이라 더 높은 해상도 소스가 없습니다.

### 5.4 라벨 가능 구간 진단 — `tools/eval/label_feasibility.py`

사람 시간을 쓰기 **전에** 어디를 라벨해야 의미가 있는지 판정합니다.
판정 경계(최소변 24px/12px)는 **산술이 아니라 이번 실측값**입니다.

| 카메라 | 반자동 가능 | 수작업까지 | 라벨 불가 |
|---|---|---|---|
| PANGYO_2 | 5% | 52% | **48%** |
| SONGDO_IC | 12% | 100% | 0% |
| SANGAM01 | 10% | 71% | 29% |
| OKRYEON_IC | 0% | 40% | **60%** |
| YEONSU_JCT | 7% | 22% | **78%** |

> 처음엔 IoU 산술로 기준을 세웠는데 "8px도 라벨 가능"이라는 답이 나왔습니다. 방금 11×8px에서
> 경계를 못 본 것과 모순이라 관측값으로 바꿨고, 도구에 출처를 명시했습니다.

---

## 6. 추적 — A/B/C 실측

`tools/retrack.py`가 검출은 그대로 두고 연관만 교체해 3원 비교합니다.

| (SONGDO_IC, max_age=90) | 트랙수 | 수명중앙 | 0.5초미만 | >180km/h 연관 |
|---|---|---|---|---|
| A. ByteTrack | 152 | 0.78s | 42% | 1271 |
| B. BEV | 66 | **5.00s** | 14% | 310 |
| C. BEV+차로구속 | 69 | 5.37s | 10% | 300 |

**차로 구속(C)은 이득이 없습니다.** 3개 지점 × 4개 max_age 설정에서 B와 차이가 노이즈
수준입니다. 직선 도로에서는 차로 coasting ≈ 직선 coasting이고, 클립이 4~15초라 긴 가림
구간 자체가 드뭅니다. `--lane` 옵션으로 남겨 뒀지만 기본은 꺼져 있습니다.

### 6.1 투영 발산 게이트

지면 투영이 지평선 근처에서 발산합니다 — **SONGDO_IC 검출의 13.7%가 300m 밖, 1.1%가 1km 밖,
최대 33km.** 투영 거리·불확실성 게이트(`--max-range 250`, `--max-sigma 25`)를 넣었습니다.
DB 적재와 `lane_snap`에도 같은 게이트가 필요합니다.

---

## 7. 학습 — 회전 카운팅 · 운동 필드

### 7.1 링크 전이 카운팅 — `tools/turn_counts.py`

맵매칭 링크 시퀀스에서 `P(next|cur)`, Dirichlet 스무딩, Wilson 95% 신뢰구간, 회전 종류 판정.
신경망 없이 세기만 하면 되고, 결과가 그대로 **방향별 회전교통량**이며 나중에 GNN의
사전분포·기준선이 됩니다.

```
PANGYO_2: 트랙 149 · 링크 2개 이상 통과 16 · 전이 관측 29건
표본 충분(≥5)한 분기: 2/22개
  ⚠ 이 확률표는 아직 예측에 쓸 수 없습니다 — 구조는 맞지만 수치가 우연에 지배됩니다.
```

회전비율이 ±10%로 수렴하려면 분기당 100건 이상이 필요합니다. 현재 관측 4초.

### 7.2 운동 필드 — `trafficlab/motion/motion_field.py`

"그 지점에서 차들이 평균적으로 얼마나 빠른가"를 학습합니다. **방향은 학습 대상이 아닙니다** —
차로 접선이 곧 방향이고 정밀도로지도가 정확히 줍니다.

**Frenet 종방향 속도(ds/dt)** 를 씁니다. 2D 평면 속도는 횡방향 지터를 그대로 먹지만,
맵매칭이 이미 (link, s, d)로 바꿔 놨으므로 종방향만 보면 잡음이 구조적으로 빠집니다.

> **추정기 결함 수정**: 프레임쌍 ds/dt를 그대로 쌓으니 지터가 만든 "거의 0" 표본이 중앙값을
> 끌어내렸습니다. **트랙×bin 단위로 먼저 중앙값**을 취해 대표값 1개만 기여시키도록 고쳤습니다.
> → SONGDO_IC 중앙 속도 44.3 → **58.9 km/h**, 가속도 95% 5.13 → **2.39 m/s²**

희소 구간은 ① 관측 → ② 링크 내 s축 보간 → ③ 링크 중앙값 → ④ 전역 순으로 메우고,
**각 칸의 출처를 `src`에 기록**합니다. 시각 품질 우선이므로 표시용 클램프(15~120 km/h)도
넣되 클램프된 칸 수를 함께 출력합니다.

**커버리지**: PANGYO_2 1.4%, SONGDO_IC 17.9%. 지금은 "그 지점의 평균"이 아니라 사실상
"이 카메라의 평균"입니다 — 녹화 시간이 늘면 그대로 개선됩니다.

### 7.3 GNN(VectorNet/LaneGCN) 데이터 요건

| | 규모 | 프로토콜 |
|---|---|---|
| Argoverse 2 | 250,000 시나리오, 2,000km, 6개 도시 | 11초(5초 관측 → 6초 예측) @10Hz |
| INTERACTION | 드론·고정카메라 + HD맵 | **1초 관측 → 3초 예측** |
| **현재** | 관측 29초 · 트랙 472개 | 중앙 지속 **1.0초** |

**병목은 총 시간이 아니라 트랙 지속시간입니다.** 영상을 1,000배 모아도 이 비율은 안 변합니다.
순서: ① 트랙 연속성 → ② HD맵·카메라 공간 일치 → ③ 볼륨.

| 목표 | 필요 데이터 |
|---|---|
| 카운팅 마르코프 | 회전쌍당 100트랙+ → 카메라당 수 시간 |
| Argoverse 사전학습 **파인튜닝** | 사용 가능 시나리오 1만 개 |
| 밑바닥 학습 | 비현실적 |

---

## 8. 표출 — deck.gl 3D 차량 + Frenet 보간

`webmap/src/vehicles.js` (신규) + `player.js` 연결. deck.gl `MapboxOverlay`(interleaved) +
`SimpleMeshLayer` 3장.

- **절차적 차량 메시** — 외부 glTF를 받지 않습니다(라이선스·네트워크 의존 회피).
  차체/캐빈 + **전조등·미등을 별도 레이어**로 그려 정면/후면이 눈에 보입니다.
- **시간축 보간** — 30fps 키프레임을 rAF에서 Catmull-Rom(위치) + 최단호(heading)로 채웁니다.
- deck.gl 초기화·렌더가 실패하면 기존 fill-extrusion 박스로 자동 폴백합니다.

**좌표 규약(여기서 틀리면 차가 옆으로 갑니다)**: `sat_coords` heading은 `atan2(dy,dx)` —
동쪽 0°, 반시계. deck.gl `orientation`은 `[pitch, yaw, roll]`이고 yaw는 z축 반시계.
메시를 +X 정면 / +Z 상방으로 만들었으므로 `yaw = heading`, `roll = 0`.

**실데이터 검증**

```
키프레임 재현 오차 최대 0.000000 m (5,503점)
프레임간 최대 이동 0.555 m (240 km/h 상당) · 메시 무결성 OK
```

**잡은 버그 3개**

1. 균일 Catmull-Rom이 프레임 결손 구간에서 오버슈트 → 간격 불균등 시 선형 폴백
2. 원본 replay의 투영 발산이 그대로 들어와 차가 수 km 순간이동 → 200km/h 초과 구간에서
   트랙을 **잇지 않고 끊습니다**(사라졌다 나타나는 편이 정직)
3. **카메라 전환 시 이동체가 안 보임** — 렌더러를 한 번만 생성하면서 첫 카메라의 `toLL`을
   클로저로 붙잡고 있었습니다. `sat_coords`는 카메라별 world 원점 기준이라 두 번째부터
   좌표가 이전 원점으로 계산돼 지도 밖에 그려졌고, 폴백 박스도 비워져 아무것도 안 보였습니다.
   → `setProjector(toLL)`로 카메라마다 갱신

```
검증(판교 → 송도IC 순서로 열기):
  A 차량 ↔ A 카메라   61 m
  B 차량 ↔ B 카메라   18 m       ← 정상
  B 차량 ↔ A 카메라   39,524 m   ← 수정 전이라면 여기 그려졌음
```

---

## 9. 실행 순서 (파이프라인)

**순서가 중요합니다.** 마스킹은 검출을 지우므로 맨 앞, 재투영은 캘리브레이션을 바꾼 뒤
반드시, 맵매칭은 그 결과 위에.

```bash
# 0) HD맵 준비 (1회)
python tools/export_hdmap_snap.py

# 1) 캘리브레이션 검수 → 필요시 자동 정합 → GCP 1~3점
python tools/lane_overlay.py     --loc PANGYO_2          # 게이트: 차선이 도로에 얹히는가
python tools/autocalib_lanes.py  --loc PANGYO_2 --apply  # 구조 정렬(QC 확인 후)
python tools/gcp_candidates.py   --loc PANGYO_2          # 클릭 지점 추천
#   → calibrate.html 에서 GCP 1~3점 (4점 이상은 자동 결과를 버림)

# 2) 정적 오탐 마스크 (카메라당 1회, 사람 승인)
python tools/static_mask.py --loc PANGYO_2 --review
python tools/static_mask.py --loc PANGYO_2 --accept 0 1

# 3) 본 파이프라인
python tools/reproject.py   --loc PANGYO_2 --source <원본 replay>      # 새 캘리브레이션 반영
python tools/static_mask.py --loc PANGYO_2 --apply output/reprojected/PANGYO_2/clip.json.gz
python tools/mapmatch.py    --loc PANGYO_2 --src <masked> --center-lane
python tools/bridge_to_webmap.py --replay output/mapmatch/PANGYO_2/clip.json.gz --loc PANGYO_2

# 4) (선택) 학습·통계
python tools/motion_field.py --loc PANGYO_2
python tools/turn_counts.py  --loc PANGYO_2
```

> ⚠ `mapmatch.py`를 `--src` 없이 돌리면 **이미 맵매칭된 웹맵 replay를 다시 읽어 이중
> 적용**됩니다. 항상 원본 계열을 지정하세요.

---

## 10. 철회한 주장

> **이 절이 이 문서에서 가장 중요합니다.** 측정 전에 옳아 보였고 측정 후에 틀린 것들입니다.

**① "BEV 재추적이 추적을 개선했다" — 철회**

수명 중앙값이 0.92 → 3.07초로 늘어난 것을 개선으로 보고했으나, GT로 재면 반대였습니다
(IDF1 0.441 → 0.390, IDSW 2 → 5). 수명 증가의 일부는 **잘못된 병합**이었고, 물리적 타당성
프록시(>180km/h 연관)로는 잡히지 않았습니다. 단, GT는 근거리 ROI만이고 BEV의 강점 주장은
원거리에 있으므로 **공정한 시험은 아닙니다** — 그리고 원거리 GT는 만들 수 없습니다(§5.3).

**② "차로 구속 예측이 추적 연속성을 개선한다" — 기각**

3개 지점 × 4개 max_age 설정에서 BEV와 차이가 노이즈 수준이고 때로 소폭 악화. 직선 도로에서는
차로 coasting ≈ 직선 coasting이라 당연합니다. `--lane` 옵션은 남겨 두되 기본 OFF입니다.

**③ "자동 정합이 판교 캘리브레이션을 개선했다" — 한 번 잘못 적용**

Chamfer 잔차가 40% 개선되어 적용했으나 투영이 화면 한가운데 소실점으로 붕괴했습니다.
git으로 되돌렸습니다. **이후로는 지표가 좋아도 육안 확인 없이 적용하지 않습니다.**

**④ "다른 카메라의 렌더 대상이 1~2대뿐이다" — 표집 오류**

한 시점만 찍어서 나온 값이었습니다. 시간축 평균은 원본 프레임당 객체 수와 거의 같습니다
(okryeon 7.8→8.2 · songdo 17.6→16.6 · pangyo 44.7→50.9). 실제 원인은 렌더러의 `toLL` 버그
하나였습니다(§8).

**⑤ "IoU 산술로 라벨 가능 크기를 정할 수 있다" — 기준 교체**

`±2px 오차면 8px 객체도 IoU 0.5 통과`라는 답이 나왔지만, 방금 11×8px에서 경계를 못 본 것과
모순이었습니다. 한계는 IoU 산술이 아니라 **육안 식별성**이라, 기준을 실측값(최소변 24px/12px)
으로 바꿨습니다.

---

## 11. 알려진 한계

| 항목 | 현황 | 영향 |
|---|---|---|
| **캘리브레이션** | 5대 중 4대가 검수 게이트 탈락. 자동 VP+IPM은 자세가 물리적으로 비현실적 | 차로 배정이 틀릴 수 있음 — **모든 하류의 선행조건** |
| 자동 정합 | 거친 정렬까지. 페인트가 횡방향을 구속하지 못함(±8.75m에서 비용 변동 3%) | GCP 1~3점 필요 |
| 원거리 GT | 만들 수 없음(11×8px, 최소변 p10 6.6px) | BEV의 원거리 우위 주장 **검증 불가** |
| 추적 | 근거리 GT에서 BEV < ByteTrack | BEV 채택을 원거리 성능으로 정당화하지 말 것 |
| 차로 고정 | 이탈은 막지만 **올바른 차로 선택은 보장 안 됨**. 차로 변경·회전 미표현 | 시각 품질 목적에는 충분 |
| 운동 필드 | 커버리지 1.4~17.9% | "지점별 평균"이 아니라 "카메라 평균" |
| 회전 카운팅 | 22개 분기 중 2개만 표본 충분 | 예측에 사용 불가 |
| 표본 | 관측 총 29초 | 학습·통계 전반의 상한 |
| 3D 렌더 육안 검증 | 개발 환경에서 미확인(빌드·데이터·좌표는 검증) | 개발 서버에서 확인 필요 |
| 합성 vs 관측 | 재시딩 방식 도입 시 위치·속도가 합성값이 됨 | `detection` 테이블에 `source` 컬럼 필요 |
| HD맵 커버리지 | 3대(PANGYO_2·YEONSU_JCT·SONGDO_L020101) | 나머지는 OSM 폴백 |

## 12. 다음 작업

1. **캘리브레이션 GCP** — `autocalib_lanes --apply` 후 카메라당 1~3점. 나머지 전부의 선행조건
2. **녹화 시간 확대** — 운동 필드 커버리지·회전 카운팅 표본·GNN 데이터가 전부 여기 묶임
3. **상암 3D 정밀도로지도** — 받아서 루트에 풀면 `export_hdmap_snap.py`가 자동으로 집어감
4. **인접 차로 전환 허용** — `r_link`/`l_link`로 제한한 차로 변경 표현
5. **원거리 라벨링** — 필요하다면 사람 주석자 + 영상 재생(정지 화면에서 안 보이는 움직임이
   재생하면 보입니다). 또는 원거리를 평가 대상에서 제외(투영 게이트가 이미 버림)
6. **`detection` 테이블에 `source: observed | simulated`** — 합성 위치가 관측처럼 보고되지 않게
7. **전면/후면 2진 분류기**(자기지도 라벨) — 대향 차로 배정 해소 · 역주행 탐지

---

## 부록 A. 산출물 위치

```
location/<LOC>/static_mask.png             정적 오탐 마스크(승인본)
location/<LOC>/gcp_candidates.json         GCP 후보(브라우저용 사본: webmap/public/data/gcp/)
webmap/public/data/hdmap/hdmap.geojson     병합 HD맵
webmap/public/data/hdmap/hdmap_align*.json 위성영상 정합(표시 전용)
output/laneoverlay/<LOC>/overlay.png       캘리브레이션 게이트 결과
output/autocalib/<LOC>/qc.png              자동 정합 QC(청록=페인트 / 빨강=전 / 초록=후)
output/staticmask/<LOC>/candidates.png     마스크 후보 크롭(승인 전 필독)
output/mapmatch/<LOC>/clip.json.gz         맵매칭 + 차로 고정 결과
output/motionfield/<LOC>/field.json        운동 필드
output/turncounts/<LOC>/turns.json         링크 전이 확률
eval/<LOC>/labels.json                     GT(방법·한계 기록 포함) · metrics.json 평가 결과
```

## 부록 B. 좌표 규약 (여기서 틀리는 일이 많습니다)

| 대상 | 규약 |
|---|---|
| `sat_coords` | world 원점 기준 **로컬 미터** — **카메라마다 원점이 다릅니다** |
| `heading` | `atan2(dy, dx)` — **동쪽 0°, 반시계** |
| MapLibre `bearing` | 북쪽 0°, 시계 → `bearing = (90 − heading + 360) % 360` |
| deck.gl `orientation` | `[pitch, yaw, roll]`, yaw = z축 반시계. 메시가 +X 정면/+Z 상방이면 `yaw = heading` |
| `GProjection.H` | **미왜곡 영상 → sat**. 지면→영상은 `H_inv` |
| 보정 합성 | `H_new = correction3 · H_old` (`homography.js` 의 `mul3` 규약) |

> **실제 사고 사례**: `vehicles.js` 가 렌더러를 한 번만 만들면서 첫 카메라의 `toLL` 을 붙잡아,
> 두 번째 카메라부터 차량이 **39 km 떨어진 곳**에 그려졌습니다(= 안 보였습니다).
> 카메라 전환 시 `setProjector()` 로 투영기를 갱신해야 합니다.

## 부록 C. 이 문서에 나온 도구

| 도구 | 역할 |
|---|---|
| `tools/export_hdmap_snap.py` | HD맵 다중 데이터셋 병합 · 주행방향 검증 |
| `tools/lane_snap.py` | 차로 정렬(그리디) · HD맵↔OSM 채점 폴백 |
| `tools/mapmatch.py` | HMM 맵매칭 · Frenet 재구성 · 차로 고정 |
| `tools/lane_overlay.py` | 캘리브레이션 검수 게이트 |
| `tools/autocalib_lanes.py` | 노면표시 자동 정합(자세/유사변환) |
| `tools/gcp_candidates.py` | GCP 클릭 지점 추천 |
| `tools/static_mask.py` | 박힌 안내문구 마스크(후보 제시 → 사람 승인) |
| `tools/hdmap_align.py` | HD맵 ↔ 위성영상 Chamfer/DT 정합(표시용) |
| `tools/motion_field.py` | 운동 필드 v(link, s) 학습 |
| `tools/turn_counts.py` | 링크 전이 · 방향별 회전교통량 |
| `tools/eval/label_feasibility.py` | 라벨 가능 구간 진단 |
| `trafficlab/motion/lane_graph.py` | 차선 그래프 + Frenet |
| `trafficlab/motion/map_matching.py` | Viterbi · 차로 고정 · 등장성 회귀 |
| `trafficlab/motion/motion_field.py` | 운동 필드 빌드/조회 |
| `webmap/src/vehicles.js` | deck.gl 3D 차량 + 트랙 보간 |
